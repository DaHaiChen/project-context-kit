# 模块上下文目录

每个业务或技术模块都应该有一个独立上下文文件。新增模块时，复制 `_template.md` 并重命名为模块名。

## 命名建议

- 使用小写英文或稳定的模块名，例如 `user.md`、`auth.md`、`graph.md`。
- 如果模块名必须使用中文，也要保持文件名稳定，避免频繁重命名。

## 模块文件必须说明

- 模块职责
- 不负责的范围
- 主要入口
- 依赖模块
- 对外提供能力
- 关键数据
- 已知坑点
- 最近重要变化
